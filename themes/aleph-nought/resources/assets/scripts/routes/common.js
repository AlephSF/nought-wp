import mobileMenuInteractions from '../util/mobileMenuInteractions'

export default {
  init() {
    // JavaScript to be fired on all pages

    mobileMenuInteractions()
  },
  finalize() {
    // JavaScript to be fired on all pages, after page specific JS is fired
  },
};
